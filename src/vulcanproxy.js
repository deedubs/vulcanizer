
class Vulcanproxy {

    addHost(fqdn, options) {

        return Promise.resolve();
    }

    addBackend(id, options) {

        return Promise.resolve();
    }

    addServer(backend, serverId, url) {

        return Promise.resolve();
    }

    removeServer(backend, serverId) {

        return Promise.resolve();
    }

    addFrontend(fqdn) {

        return Promise.resolve();
    }
}

module.exports = Vulcanproxy;
